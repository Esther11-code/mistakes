import 'package:flutter/material.dart';

class MentorGoal extends StatelessWidget {
  const MentorGoal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}